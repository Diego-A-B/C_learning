//Statements
//Ohms law

#include <stdio.h>
#include <math.h>

int main()
{
    int voltage, current, resistance;
    char missing;

    printf("What is missing? voltage(v), current(i), or resistance(r)?\nEnter the value as the single letter in parenthases.");
    scanf("%c",&missing);

    if (missing == 'v')
    {

    }

}